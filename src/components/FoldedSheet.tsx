import React, { ReactElement } from 'react'
import SheetSummary from '../models/SheetSummary'

export interface FoldedSheetProps {
    data: SheetSummary
}

export default function FoldedSheet(props: FoldedSheetProps): ReactElement {
    return (
        <div>
            {}
        </div>
    )
}
